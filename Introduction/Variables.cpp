#include <iostream>
using namespace std;

int main()
{
    // declaring the variable

    int number;
    double decimal;
    char character;
    string name;
    bool what;
    const float pi = 3.2;

    // initilization of variables
    // type variableName = value;

    int age = 21;      // Integer inmbers
    double salary = 1.2; // Double for decimal numbers
    char character1 = 'c'; // For character use single quote
    string name = "sanzgrapher";
    bool result = true; // Bol for true or false

    // Displaying variables
    cout << "I am " << name << " My age is " << number << " is a " << bool(what) << " statement\n";
}
